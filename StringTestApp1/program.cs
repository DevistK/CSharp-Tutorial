const string userName = "Bob";
const int messageCount = 3;
const float temperature = 34.4f;

Console.Write(
    $"Hello {userName}! You have {messageCount} messages in your inbox. The temperature is {temperature} celsius."
);
